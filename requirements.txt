# Runtime dependencies (Windows only -- this tool reads/writes another
# process's memory, which is a Windows-specific operation).
pymem>=1.13.0
psutil>=5.9.0          # optional, nicer process enumeration; core/process.py
                         # falls back to a raw Toolhelp32 walk if this is absent

# Only needed if you run tools/build_signatures.py yourself (e.g. after
# adding a new signature) -- NOT needed to run the mod menu itself, since
# data/signatures.json is already generated and committed.
# keystone-engine>=0.9.2

# Only needed to build the standalone .exe yourself (build.bat does this
# for you, or let the GitHub Actions workflow build it on a tag push).
# pyinstaller>=6.0
